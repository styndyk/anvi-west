// Знаходимо всі посилання з класом 'products-section__block'
// const productLinks = document.querySelectorAll('.products-section__block');

// // Додаємо обробник подій для кожного посилання
// productLinks.forEach(link => {
//   link.addEventListener('click', (event) => {
//     const itemId = link.getAttribute('data-item'); // Отримуємо ідентифікатор товару з атрибуту data-item
//     // Формуємо URL з параметром "item" і переходимо на сторінку з товаром
//     window.location.href = `products?item=${itemId}`;
//   });
// });
